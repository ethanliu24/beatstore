<% title(t("label.sign_up")) %>

<div class="auth-form">
  <h2 class="font-bold text-center"><%= t("label.sign_up") %></h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |form| %>
    <%= render Ui::AuthInputComponent.new(
      form:, resource:, field: :email, label: t("label.email"), type: :email,
      options: { autofocus: true, autocomplete: "email" },
    ) %>

    <%= render Ui::AuthInputComponent.new(
      form:, resource:, field: :display_name, label: t("label.display_name"), type: :text,
      options: { autocomplete: "display_name" },
    ) %>

    <%= render Ui::AuthInputComponent.new(
      form:, resource:, field: :password, label: t("label.password"), type: :password,
      options: { autocomplete: "new-password" }
    ) %>

    <%= render Ui::AuthInputComponent.new(
      form:, resource:, field: :password_confirmation, label: t("label.password_confirmation"), type: :password,
      options: { autocomplete: "new-password" }
    ) %>

    <%# TODO link to privacy page %>
    <p class="text-[0.55rem] mt-12 mb-1 font-light text-center">
      <%= t("authentication.agree_tos_and_privacy_html",
        tos_link: link_to(t("label.terms_of_service"), terms_of_service_path,
          target: "_blank", rel: "noopener noreferrer", class: "underline text-accent").html_safe,
        privacy_link: link_to(t("label.privacy_policy"), root_path,
          target: "_blank", rel: "noopener noreferrer", class: "underline text-accent").html_safe
      ) %>
    </p>

    <div class="actions button py-0.5">
      <%= form.submit t("label.sign_up"), class: "w-full cursor-pointer" %>
    </div>

    <a href=<%= new_confirmation_path(resource_name) %> class="external-link" target="_blank" rel="noopener noreferrer">
      <%= t("authentication.no_confirmation_instructions") %>
    </a>
  <% end %>

  <%= render "users/shared/omniauth_login" %>

  <p class="text-[0.6rem] my-1 font-light text-center">
    <%= t("authentication.already_signed_up") %>
    <a href=<%= new_session_path(resource_name) %> class="external-link"><%= t("label.sign_in") %></a>
  </p>
</div>

