<div class="flex flex-col justify-start items-center gap-4 w-full py-2">
  <% comments.each do |comment| %>
    <%= render partial: "comments/comment", locals: { comment: comment } %>
  <% end %>
</div>
