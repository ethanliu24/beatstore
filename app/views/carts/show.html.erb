<% content_for :title, t("cart.tab_title") %>

<%= render PageTitleComponent.new(
  title: t("cart.show.title"),
  subtitle: t("cart.show.subtitle")
) %>

<div id="cart" class="w-full">
  <%= render partial: "carts/items_list" %>
</div>
