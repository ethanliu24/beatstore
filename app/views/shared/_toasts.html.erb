<div class="flex flex-col items-end space-y-4">
  <div class="fixed bottom-0 right-0 z-40 mr-8 mb-8">
    <% flash.each do |type, message| %>
      <%= render Ui::ToastComponent.new(type:, message:) %>
    <% end %>
  </div>
</div>
