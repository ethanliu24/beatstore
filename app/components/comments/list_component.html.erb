<div class="flex flex-col justify-start items-center gap-6 w-full py-3">
  <% comments.each do |comment| %>
    <%= render Comments::CommentComponent.new(comment:, current_user:) %>
  <% end %>
</div>
