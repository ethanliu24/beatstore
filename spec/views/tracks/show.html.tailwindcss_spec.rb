require 'rails_helper'

RSpec.describe "tracks/show", type: :view do
  before(:each) do
    assign(:track, create(:track))
  end

  it "renders attributes in <p>" do
    skip "add some examples to (or delete) #{__FILE__}"
  end
end
