host: "<%= ENV.fetch("APP_HOST") %>"
stripe:
  public_key: "<%= Rails.application.credentials.dig(:stripe, :public_key) %>"
  secret_key: "<%= Rails.application.credentials.dig(:stripe, :secret_key) %>"
  payments_webhook_secret: "<%= Rails.application.credentials.dig(:stripe, :payments_webhook_secret) %>"
oauth2:
  google:
    client_id: "<%= Rails.application.credentials.dig(:oauth2, :google, :client_id) %>"
    client_secret: "<%= Rails.application.credentials.dig(:oauth2, :google, :client_secret) %>"
google:
  api:
    oauth:
      client_id: "<%= Rails.application.credentials.dig(:google, :api, :oauth, :client_id) %>"
      client_secret: "<%= Rails.application.credentials.dig(:google, :api, :oauth, :client_secret) %>"
      google_drive_refresh_token: "<%= Rails.application.credentials.dig(:google, :api, :oauth, :google_drive_refresh_token) %>"
backup:
  google_drive:
    service_account: "<%= Rails.application.credentials.dig(:backup, :google_drive, :service_account) %>"
    folders:
      primary: "1WcfCWe7GSrL9mZw79Ti20HGsTKoLqpnZ"
      queue: "1MBo6EZF15R6KoEFK3X6U8RUnfeZOXD-R"
      cable: "1A1TCiO7cGlVQOAJAt5L-NYE1PYqvvbC5"
      cache: "1iw5N_tvfIpWptEHudWSEM3MnY3uKyh9p"
      errors: "1jt8rqfy_EYW5uMN5B1N8C3ZWVRkLbPnr"
